import type { Metadata } from 'next';

import AlertsPage from './page-view';

export const metadata: Metadata = {
  title: 'Alerts',
  description: 'Manage product change alerts and notifications.',
};

export default AlertsPage;
