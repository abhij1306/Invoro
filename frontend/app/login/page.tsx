import type { Metadata } from 'next';

import LoginPage from './page-view';

export const metadata: Metadata = {
  title: 'Sign In',
  description: 'Sign in to the Invoro extraction platform.',
};

export default LoginPage;
