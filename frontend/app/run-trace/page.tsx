import type { Metadata } from 'next';

import RunTracePage from './run-trace-page';

export const metadata: Metadata = {
  title: 'Run Trace',
  description: 'Inspect the execution trace and diagnostics for a crawl run.',
};

export default RunTracePage;
