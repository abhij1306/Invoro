import type { Metadata } from 'next';

import MonitorsPage from './page-view';

export const metadata: Metadata = {
  title: 'Monitors',
  description: 'Schedule and manage recurring crawl monitors.',
};

export default MonitorsPage;
