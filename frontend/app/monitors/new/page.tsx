import type { Metadata } from 'next';

import NewMonitorPage from './page-view';

export const metadata: Metadata = {
  title: 'New Monitor',
  description: 'Create a new scheduled crawl monitor.',
};

export default NewMonitorPage;
