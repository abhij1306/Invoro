import type { Metadata } from 'next';

import RunsPage from './page-view';

export const metadata: Metadata = {
  title: 'Run History',
  description: 'View and manage the history of crawl runs.',
};

export default RunsPage;
