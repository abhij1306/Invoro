import type { Metadata } from 'next';

import SelectorsPage from './page-view';

export const metadata: Metadata = {
  title: 'Selectors',
  description: 'Manage domain selector memory and extraction rules.',
};

export default SelectorsPage;
