import type { Metadata } from 'next';

import RegisterPage from './page-view';

export const metadata: Metadata = {
  title: 'Register',
  description: 'Account registration for the Invoro extraction platform.',
};

export default RegisterPage;
