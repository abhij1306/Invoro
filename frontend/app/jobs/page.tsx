import type { Metadata } from 'next';

import JobsPage from './page-view';

export const metadata: Metadata = {
  title: 'Jobs',
  description: 'Browse and manage crawled job postings.',
};

export default JobsPage;
