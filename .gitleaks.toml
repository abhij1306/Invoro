title = "Invoro secret scanning"

[extend]
useDefault = true

[allowlist]
description = "Browser sessionStorage namespaces are not credentials"
regexTarget = "line"
regexes = [
  '''BROKEN_THUMBNAIL_(URLS|HOSTS)_STORAGE_(KEY|NAME)\s*=\s*['"]invoro-broken-thumb-(urls|hosts)-v1['"]''',
]
