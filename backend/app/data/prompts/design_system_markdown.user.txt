Policy:
$system_policy

Source URLs:
$source_urls_json

Deterministic design tokens:
$design_tokens_json

Deterministic draft:
$deterministic_markdown

Return this JSON object:
{
  "overview": "one short paragraph describing the extracted visual system",
  "usage_notes": ["short implementation notes based only on the supplied tokens"],
  "caveats": ["short caveats about sampling limits or missing evidence"]
}
